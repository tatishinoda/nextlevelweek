// Para json-server, os dados iniciais estão em db.json na raiz do projeto
// Este arquivo agora é apenas um placeholder para compatibilidade

function seedDatabase() {
	console.log('✅ Banco de dados (json-server) já inicializado com db.json')
}

module.exports = seedDatabase

module.exports = seedDatabase
